<!DOCTYPE html>
<html>
    <head>
        <%- include('../partials/head') %>
    </head>
    <body>
       <%- include('../partials/nav', { active: 'ask', userData: user }) %>
        <div class="container" id="ask-parent">
            <div class="col" id="side">

            </div>

            <div class="row" id="ask-head">
                <div id="ask-head-text">
                    <h1 id="ask-title"><b>Ask a public question</b></h1>               
                </div>
            </div>
            <div class="row" id="form-parent">
                <div class="col w-75 p-3" id="form">
                    <form id="wack-form" method="POST" action="/ask">  <!-- ohne action vlt-->
                        <div class="form-group">
                            <input type="hidden" name="UserID" value="<%= user.id %>">
                            <h4 id="form-h4">Title</h4>
                            <p>Be specific and imagine you're asking a question to another person</p>
                            <textarea name="title" class="form-control" id="wack-form-text" placeholder="e.g how to hack NASA using HTML" maxlength="250"></textarea>
                        </div>
                        <div class="form-group">
                            <h4 id="form-h4">Body</h4>
                            <p>Include all the information someone would need to answer your question</p>
                            <textarea name="body" class="form-control" id="wack-form-text" rows="10"></textarea>
                        </div>
                        <div class="form-group">
                            <h4 id="form-h4">Tag</h4>
                            <p>Tags are used to easier find your question</p>
                            <textarea name="tag" class="form-control" id="wack-form-text" maxlength="50"></textarea>
                        </div>
                        <button id="ask-submit" type="submit" class="btn btn-dark">Ask Question!</button>
                    </form>
                </div>
                <div class="col-offset-5 h-25 p-3" id="guide-parent">             
                     <div class="guide">
                         <p id="guide-text">The community is here to help you with specific coding, algorithm, or language problems.
                            Avoid asking opinion-based questions.</p>
                     </div>                               
                </div>
            </div>
        </div>
    </body>
</html>